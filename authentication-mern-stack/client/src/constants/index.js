import { BREADCRUMB } from './breadcrumb';

const APP_KEYS = {
  ACCESS_TOKEN: '_auth_a_t_',
  REFRESH_TOKEN: '_auth_r_t_',
  REMEMBER_LOGIN: '_auth_r_l_',
};

const ROUTES = {
	LOGIN: '/login',
	HOME: '/',
};

export { BREADCRUMB, APP_KEYS, ROUTES };